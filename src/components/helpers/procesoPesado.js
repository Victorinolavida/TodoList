export default function procesoPesado(numIteraciones) {
  for (let i = 0; i < numIteraciones; i++) {
    console.log("hay vamos............");
  }
  return `${numIteraciones} iteraciones finalizadas.`;
}
